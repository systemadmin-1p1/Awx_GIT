---
- name: APT Update and Upgrade
  hosts: all
  become: true
  gather_facts: false

  tasks:
    - name: Update APT cache
      ansible.builtin.apt:
        update_cache: true

    - name: Upgrade all packages
      ansible.builtin.apt:
        upgrade: dist
