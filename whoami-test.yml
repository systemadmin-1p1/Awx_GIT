---
- name: Update apt cache and upgrade packages
  hosts: all
  become: yes
  tasks:
    - name: Update apt cache and upgrade packages
      ansible.builtin.apt:
        update_cache: yes
        upgrade: dist
        cache_valid_time: 0
      register: apt_output
      until: apt_output is success
      retries: 5
      delay: 10
